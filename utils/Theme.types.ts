import { ColorValue } from "react-native";

export type Theme = {
    primary: ColorValue,
    secondary: ColorValue,
    border: ColorValue,
    body: ColorValue,
    light: ColorValue,
    dark: ColorValue,
    text: ColorValue
};